For each entity do the following:
	Create Model (singular 'User')
	create folder for views  (in singular lowercase folder named of object 'user') 
	Create Views (at least...Edit and List)
	create Controller (plural 'Users')
		add store, view, and model to controller (don't forget this, doh!)
	create store (this is plural of object 'Users')
Add action to menu item in Navigation
Add navigation to appcontroller to display list
Add Controller to app.js
Create server side controller (in singular form 'UserController.cs')